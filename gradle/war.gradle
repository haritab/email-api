apply plugin: "war"

bootWar {
    mainClassName = "com.viacom.email.EmailApiApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]
}
